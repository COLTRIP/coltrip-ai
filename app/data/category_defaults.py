"""
카테고리별 면적 기본값.

RealDataLoader가 TourAPI에서 못 가져오는 필드(area_m2)를
채울 때 사용하는 임시 기본값 표입니다. category_codes.py의 30개 카테고리와
정확히 이름이 일치해야 합니다.

값의 근거: 해당 카테고리의 일반적인 규모를 참고해 임의로 추정한 값입니다
(실측치 아님). 실제 상가업소정보 API 연동 전까지의 잠정치입니다.
"""
from __future__ import annotations

CATEGORY_DEFAULTS: dict[str, dict[str, float]] = {
    # 자연 계열
    "해수욕장": {"area_m2": 150000},
    "자연경관(산)": {"area_m2": 100000},
    "자연경관(하천‧해양)": {"area_m2": 80000},
    "자연생태": {"area_m2": 50000},
    "자연공원": {"area_m2": 100000},
    "기타자연관광": {"area_m2": 50000},

    # 역사·종교 계열
    "역사유적지": {"area_m2": 15000},
    "역사유물": {"area_m2": 3000},
    "종교성지": {"area_m2": 30000},
    "안보관광지": {"area_m2": 10000},

    # 문화·도시 계열
    "랜드마크관광": {"area_m2": 5000},
    "테마공원": {"area_m2": 200000},
    "도시공원": {"area_m2": 20000},
    "도시지역문화관광": {"area_m2": 3000},
    "복합관광시설": {"area_m2": 30000},
    "공연시설": {"area_m2": 5000},
    "전시시설": {"area_m2": 8000},
    "행사시설": {"area_m2": 15000},
    "교육시설": {"area_m2": 5000},
    "기타문화관광지": {"area_m2": 3000},

    # 체험 계열
    "전통체험": {"area_m2": 5000},
    "공예체험": {"area_m2": 1000},
    "농산어촌체험": {"area_m2": 20000},
    "산사체험": {"area_m2": 20000},
    "웰니스관광": {"area_m2": 3000},
    "산업관광": {"area_m2": 10000},
    "기타체험": {"area_m2": 5000},

    # 새로 분리된 것 + 카페
    "도서관": {"area_m2": 2000},
    "서점": {"area_m2": 800},
    "카페": {"area_m2": 900},
}

# 혹시 매핑에 없는 카테고리가 들어올 경우의 안전장치
_FALLBACK = {"area_m2": 5000.0}


def get_category_defaults(category: str) -> dict[str, float]:
    """카테고리명으로 면적 기본값을 반환합니다. 없으면 무난한 기본값(FALLBACK)."""
    return CATEGORY_DEFAULTS.get(category, _FALLBACK)